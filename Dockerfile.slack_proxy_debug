FROM python:3.12-slim

WORKDIR /app

COPY tools/slack_proxy_debug.py ./

ENV PYTHONDONTWRITEBYTECODE=1 \
    PYTHONUNBUFFERED=1

RUN useradd -r -u 1001 -s /bin/false appuser && \
    chown -R appuser /app

USER appuser

EXPOSE 8081

CMD ["python", "slack_proxy_debug.py"]
