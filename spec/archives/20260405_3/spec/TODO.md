# ToDo List

- [x] `features/src/` ディレクトリの作成
- [x] `features/api`, `features/domain`, `features/worker`, `features/infrastructure` を `features/src/` 直下に移動
- [x] `features/plugins` ディレクトリを `features/cmds` に変更
- [x] pytest の実行による全体テストの最終デグレ確認
